#footer-wrap
  if theme.footer.owner.enable
    - var now = new Date()
    - var nowYear = now.getFullYear()
    if theme.footer.owner.since && theme.footer.owner.since != nowYear
      .copyright!= `&copy;${theme.footer.owner.since} - ${nowYear} <i style="color:#FF6A6A" class="fa fa-heartbeat"></i> ${config.author}`
    else
      .copyright!= `&copy;${nowYear} <i style="color:#FF6A6A" class="fa fa-heartbeat"></i> ${config.author}`
  if theme.footer.copyright&&theme.footer.custom_info!=null
    .framework-info!=`${theme.footer.custom_info}`
  if theme.footer.custom_text
    .footer_custom_text!=`${theme.footer.custom_text}`
